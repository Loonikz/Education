const {
    allPerson,
    averageAge,
    lastNameList,
    lastNameCounter,
    findLetterInLastName,
    findHomeless,
    findPplByStreet,
    streetListCounter,
    streetsLength,
    streetsThreePpl
} = require('../app')

describe('Tests for allPerson', () => {
    test('Should be undefined', () => {
        expect(allPerson()).toBeUndefined()
    })
})
describe('Tests for averageAge', () => {
    test('Should be undefined', () => {
        expect(averageAge()).toBeUndefined()
    })
})
describe('Tests for lastNameList', () => {
    test('Should be undefined', () => {
        expect(lastNameList()).toBeUndefined()
    })
})
describe('Tests for lastNameCounter', () => {
    test('Should be undefined', () => {
        expect(lastNameCounter()).toBeUndefined()
    })
})
describe('Tests for findLetterInLastName', () => {
    test('Should be undefined', () => {
        expect(findLetterInLastName()).toBeUndefined()
    })
})
describe('Tests for findHomeless', () => {
    test('Should be undefined', () => {
        expect(findHomeless()).toBeUndefined()
    })
})
describe('Tests for findPplByStreet', () => {
    test('Should be undefined', () => {
        expect(findPplByStreet()).toBeUndefined()
    })
})
describe('Tests for streetListCounter', () => {
    test('Should be undefined', () => {
        expect(streetListCounter()).toBeUndefined()
    })
})
describe('Tests for streetsLength', () => {
    test('Should be undefined', () => {
        expect(streetsLength()).toBeUndefined()
    })
})
describe('Tests for streetsThreePpl', () => {
    test('Should be undefined', () => {
        expect(streetsThreePpl()).toBeUndefined()
    })
})